// 진행 중 draft가 있으면 이어서, 없으면 /basic 으로 보내는 생성 마법사 resolver.
// meetingId는 아직 없다. 분기 로직은 후속 작업에서 구현한다.
export default function CreateMeetingResolverPage() {
  return <main>meetings/new resolver placeholder</main>;
}
