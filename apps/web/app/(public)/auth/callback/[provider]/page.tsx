// `[provider]`는 URL에서 `kakao`, `apple`처럼 소문자로 표현하는 OAuth 공급자 키다.
// 지원 공급자 검증과 콜백 처리는 후속 작업에서 구현한다.
export default function OAuthCallbackPage() {
  return <main>OAuth callback placeholder - ACC-01-F03, ACC-01-F04</main>;
}
