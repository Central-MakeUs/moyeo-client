/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * Moyeo API
 * CMC Moyeo MVP server API
 * OpenAPI spec version: v1
 */
import type { CreateMeetingRequestPlanningType } from './createMeetingRequestPlanningType';
import type { CreateMeetingRequestScheduleInputType } from './createMeetingRequestScheduleInputType';
import type { DepartureRequest } from './departureRequest';
import type { ScheduleResponseRequest } from './scheduleResponseRequest';

/**
 * 필수 JSON 파트. FormData 문자열이 아닌 application/json Blob으로 전송합니다.
 */
export interface CreateMeetingRequest {
  /**
   * 모임 카드와 초대 화면에 표시할 이름입니다.
   * @minLength 1
   * @maxLength 15
   */
  name: string;
  /**
   * 모임에 대한 간단한 설명입니다. 선택 입력이며 비워둘 수 있습니다.
   * @minLength 0
   * @maxLength 100
   */
  description?: string;
  /**
   * 필수 입력인 최대 참여 인원입니다. 방장을 포함합니다.
   * @minimum 2
   * @maximum 20
   */
  maxParticipants: number;
  /**
   * 모임 생성 FAB에서 선택한 생성 유형입니다.
   * <ul>
   *   <li>SCHEDULE_ONLY: 일정만 정하기. 날짜만 또는 날짜와 시간을 받는 조율(VOTE) 방식입니다.</li>
   *   <li>PLACE_ONLY: 장소만 정하기. 장소는 중간지점 추천(RECOMMEND) 대상으로 생성합니다.</li>
   *   <li>SCHEDULE_AND_PLACE: 일정과 장소 둘 다 정하기. 일정 조율(VOTE)과 중간지점 장소 추천(RECOMMEND)을 함께 저장합니다.</li>
   * </ul>
   * 장소 추천 전략은 서버가 결정하며, 생성 요청에서 받지 않습니다.
   * 확정 일정/확정 장소 직접 입력(FIXED)은 이번 MVP 생성 요청에서 받지 않으며, 추후 회의에서 재검토합니다.
   */
  planningType: CreateMeetingRequestPlanningType;
  /** 일정을 정하는 모임에서 선택한 입력 유형입니다. PLACE_ONLY에서는 보내지 않습니다. */
  scheduleInputType?: CreateMeetingRequestScheduleInputType;
  /** DATE_AND_TIME일 때 모든 후보 날짜에 공통으로 적용할 시작 시간입니다. 1시간 단위로 입력하며 DATE_ONLY와 PLACE_ONLY에서는 보내지 않습니다. */
  availableStartTime?: string;
  /** DATE_AND_TIME일 때 모든 후보 날짜에 공통으로 적용할 종료 시간입니다. 시작 시간보다 뒤여야 하고 1시간 단위이며 DATE_ONLY와 PLACE_ONLY에서는 보내지 않습니다. */
  availableEndTime?: string;
  /** 방장이 정한 일정 후보 날짜입니다. 일정 조율 모임에서 필수이며 PLACE_ONLY에서는 보내지 않습니다. */
  scheduleCandidateDates?: string[];
  /** 방장의 일정 응답입니다. DATE_AND_TIME에서 availableTimeRanges를 보내고 DATE_ONLY에서는 생략합니다. */
  scheduleResponse?: ScheduleResponseRequest;
  /** 방장의 출발지와 이동수단입니다. 장소 조율 모임에서 필수입니다. */
  departure?: DepartureRequest;
  /**
   * noDeadline가 false이거나 생략된 경우 필수입니다. 생성 요청 처리 시점부터 마감까지 남은 시간(분)이며, 10분 단위로 최소 10분, 최대 72시간입니다. noDeadline가 true이면 생략하거나 null로 보냅니다.
   * @minimum 10
   * @maximum 4320
   */
  deadlineMinutes?: number;
  /** true이면 참여/응답 마감 없이 모임을 생성합니다. 이 경우 deadlineMinutes는 생략하거나 null로 보냅니다. 생략하거나 false이면 deadlineMinutes가 필요합니다. */
  noDeadline?: boolean;
}
