/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * Moyeo API
 * CMC Moyeo MVP server API
 * OpenAPI spec version: v1
 */
import type { MeetingViewResponsePlaceMode } from './meetingViewResponsePlaceMode';
import type { MeetingViewResponsePlaceRecommendationStrategy } from './meetingViewResponsePlaceRecommendationStrategy';
import type { MeetingViewResponsePlanningType } from './meetingViewResponsePlanningType';
import type { MeetingViewResponseScheduleInputType } from './meetingViewResponseScheduleInputType';
import type { MeetingViewResponseScheduleMode } from './meetingViewResponseScheduleMode';
import type { ParticipantResponse } from './participantResponse';

/**
 * 모임 현황 조회 응답
 */
export interface MeetingViewResponse {
  /** 모임 ID */
  meetingId?: number;
  /** 모임명 */
  name?: string;
  /**
   * 모임 설명. 입력하지 않은 경우 null입니다.
   * @nullable
   */
  description?: string | null;
  /**
   * 이미 저장된 커버 이미지를 <img src>에 표시할 때 쓰는 상대 API 경로입니다. API 서버 주소를 앞에 붙여 사용하며, v 값은 캐시 갱신용이므로 변경하지 않습니다. 커버가 없으면 null입니다.
   * @nullable
   */
  coverImageUrl?: string | null;
  /** 모임 생성 유형 */
  planningType?: MeetingViewResponsePlanningType;
  /** 모임의 최종 확정 여부입니다. 일정과 장소 조율이 모두 필요한 모임은 두 항목이 모두 확정된 경우에만 true입니다. */
  meetingConfirmed?: boolean;
  /** 일정 설정 방식입니다. 현재 MVP에서는 일정 조율 모임이면 VOTE, 일정 조율이 없으면 NONE을 반환합니다. */
  scheduleMode?: MeetingViewResponseScheduleMode;
  /** 일정 참여 입력 유형입니다. */
  scheduleInputType?: MeetingViewResponseScheduleInputType;
  /** 장소 설정 방식입니다. 현재 MVP에서는 장소 조율 모임이면 RECOMMEND, 장소 조율이 없으면 NONE을 반환합니다. */
  placeMode?: MeetingViewResponsePlaceMode;
  /**
   * 장소 추천 방식입니다. placeMode가 RECOMMEND가 아닌 경우 null입니다.
   * @nullable
   */
  placeRecommendationStrategy?: MeetingViewResponsePlaceRecommendationStrategy;
  /** 최대 참여 인원. 방장을 포함합니다. */
  maxParticipants?: number;
  /** 현재 참여 인원. 방장을 포함합니다. */
  participantCount?: number;
  /**
   * 모임 참여/응답 마감 일시입니다. 마감 없는 모임에서는 null입니다.
   * @nullable
   */
  deadlineAt?: string | null;
  /**
   * 현재 서버 시간 기준 마감까지 남은 분입니다. 이미 마감된 경우 0이며, 마감 없는 모임에서는 null입니다.
   * @nullable
   */
  remainingMinutes?: number | null;
  /** 참여자 목록. 방장이 먼저 오고 이후 참여 순서로 정렬됩니다. */
  participants?: ParticipantResponse[];
}
