/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * Moyeo API
 * CMC Moyeo MVP server API
 * OpenAPI spec version: v1
 */
import type { MeetingInvitationResponsePlaceMode } from './meetingInvitationResponsePlaceMode';
import type { MeetingInvitationResponsePlaceRecommendationStrategy } from './meetingInvitationResponsePlaceRecommendationStrategy';
import type { MeetingInvitationResponsePlanningType } from './meetingInvitationResponsePlanningType';
import type { MeetingInvitationResponseScheduleInputType } from './meetingInvitationResponseScheduleInputType';
import type { MeetingInvitationResponseScheduleMode } from './meetingInvitationResponseScheduleMode';
import type { ParticipationStatusResponse } from './participationStatusResponse';

/**
 * 초대 코드 모임 조회 응답
 */
export interface MeetingInvitationResponse {
  /** 서버에서 모임을 식별하는 ID */
  meetingId?: number;
  /** 초대 링크 진입 화면에 표시할 모임 이름 */
  name?: string;
  /** 초대 링크 진입 화면에 표시할 모임 설명. 입력하지 않은 경우 null입니다. */
  description?: string;
  /** 이미 저장된 커버 이미지를 <img src>에 표시할 때 쓰는 상대 API 경로입니다. API 서버 주소를 앞에 붙여 사용하며, v 값은 캐시 갱신용이므로 변경하지 않습니다. 커버가 없으면 null입니다. */
  coverImageUrl?: string;
  /** 최대 참여 인원. 방장을 포함합니다. */
  maxParticipants?: number;
  /**
   * 모임 생성 유형입니다.
   * <ul>
   *   <li>SCHEDULE_ONLY: 일정만 정하기</li>
   *   <li>PLACE_ONLY: 장소만 정하기</li>
   *   <li>SCHEDULE_AND_PLACE: 일정과 장소 모두 정하기</li>
   * </ul>
   */
  planningType?: MeetingInvitationResponsePlanningType;
  /**
   * 일정 설정 방식입니다.
   * <ul>
   *   <li>VOTE: 일정 조율</li>
   *   <li>NONE: 일정 없음</li>
   * </ul>
   * 현재 MVP 생성 API에서는 FIXED 직접 입력을 받지 않습니다.
   */
  scheduleMode?: MeetingInvitationResponseScheduleMode;
  /** 일정 참여 입력 유형입니다. */
  scheduleInputType?: MeetingInvitationResponseScheduleInputType;
  /** 일정 후보 날짜 목록. scheduleMode=VOTE일 때 사용하며, scheduleMode=NONE이면 빈 배열입니다. */
  scheduleCandidateDates?: string[];
  /** 일정 조율 시작 시간. scheduleInputType=DATE_AND_TIME일 때만 값이 있습니다. */
  availableStartTime?: string;
  /** 일정 조율 종료 시간. scheduleInputType=DATE_AND_TIME일 때만 값이 있습니다. */
  availableEndTime?: string;
  /**
   * 장소 설정 방식입니다.
   * <ul>
   *   <li>RECOMMEND: 장소 추천/확정 플로우에서 장소를 정함</li>
   *   <li>NONE: 장소 없음</li>
   * </ul>
   * 현재 MVP 생성 API에서는 FIXED 직접 입력을 받지 않습니다.
   */
  placeMode?: MeetingInvitationResponsePlaceMode;
  /**
   * 생성 시 선택한 장소 추천 방식입니다. placeMode=RECOMMEND일 때만 값이 있습니다.
   * 1차 MVP에서는 생성 후 변경하지 않으며, 생성 시점에는 추천 결과나 확정 장소를 만들지 않습니다.
   * <ul>
   *   <li>MIDDLE_POINT: 참여자 출발지를 기준으로 추후 중간지점 추천 진행</li>
   *   <li>RANDOM: 추후 랜덤 방식으로 장소 추천 진행</li>
   * </ul>
   */
  placeRecommendationStrategy?: MeetingInvitationResponsePlaceRecommendationStrategy;
  /** 서버가 계산한 모임 참여/응답 마감 일시입니다. 마감 없는 모임에서는 반환하지 않습니다. */
  deadlineAt?: string;
  /** 현재 참여 인원. 방장을 포함합니다. */
  participantCount?: number;
  /** 방장으로 표시할 닉네임 */
  hostNickname?: string;
  /** 초대 링크 진입 시점의 참여 가능 상태. 참여하기 버튼 활성화와 안내 문구에 사용합니다. */
  participationStatus?: ParticipationStatusResponse;
}
